
import React from 'react';

const Header = () => {
  const trackerName ='Job Application Tracker';
  console.log(trackerName);
  return (
    <header>
      <h1>{trackerName}</h1>
    </header>
  );
};

export default Header;
